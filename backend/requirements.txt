# FastAPI & Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
httpx>=0.28.0
websockets>=14.0
python-multipart>=0.0.20
slowapi>=0.1.9

# Database & ORM
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
alembic>=1.13.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
cryptography>=44.0.0

# Configuration & Environment
# Pydantic 3 removes the deprecated class-based config still used in schemas/.
pydantic>=2.0.0,<3.0
pydantic-settings>=2.0.0,<3.0
python-dotenv>=1.0.1

# Scheduling
apscheduler>=3.10.0

# Optional horizontal scaling (enabled via REDIS_URL / ANALYSIS_QUEUE_MODE)
redis>=5.0,<7.0
arq>=0.26,<0.27

# LangChain & LLM Integration (from CLI)
# The LLM stack breaks across majors; cap at the next major above what the
# test suite currently passes with (langchain-core/langgraph 1.x) so a
# surprise 2.x release cannot land unreviewed.
langchain-core>=0.3.81,<2.0
langchain-anthropic>=0.3.15,<2.0
langchain-experimental>=0.3.4,<1.0
langchain-google-genai>=4.0.0,<5.0
langchain-openai>=0.3.23,<2.0
langchain-mistralai>=1.1.0,<2.0
langchain-groq>=1.1.0,<2.0
langgraph>=0.4.8,<2.0
langgraph-checkpoint-sqlite>=2.0.0,<4.0

# Vector memory (episodic + inter-agent Q&A)
pinecone>=5.0.0

# Trading & Data Analysis
pandas>=2.3.0
numpy>=2.0.0
yfinance>=0.2.63
stockstats>=0.6.5
mplfinance>=0.12.10b0
matplotlib>=3.9.0

# Observability
prometheus-client>=0.20.0

# Utilities
requests>=2.32.4
pytz>=2025.2
python-dateutil>=2.8.2
setuptools>=80.9.0
typing-extensions>=4.14.0
aiosqlite

# Testing
pytest>=8.0.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
pytest-mock>=3.14.0

